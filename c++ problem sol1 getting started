#include <bits/stdc++.h>
using namespace std;

int main() 
{
 int a = -5;          // Integer variable
 cout<<a<<endl;

 float b = 4.0;         // Float variable
 cout<<b<<endl;

 string s = "abcd" ;  // String variable
 cout<<s<<endl;

 int ar[4]={7,8,9,10};   // Integer array
cout<<ar[2];

  return 0;
}
..................................
#include <bits/stdc++.h>
using namespace std;

int main() 
{
 int N;
 cin>>N;
 cout<<N;

}
............................
#include <bits/stdc++.h>
using namespace std;

int main() 
{
 int A,B,C;
 cin>>A>>B>>C;
 cout<<A<<" "<<B<<" "<<C;
  return 0;
}
..............................
#include <bits/stdc++.h>
using namespace std;

int main() 
{
 int A, B, C, D, E, F, G, H, I;
 cin>>A>>B;
 cin>>C>>E;
 cin>>H>>I;
 cout<<A<<" "<<B<<" "<<C<<" "<<D<<" "<<E<<" "<<F<<" "<<G<<" "<<H<<" "<<I;
  return 0;
}
...............................
#include <bits/stdc++.h>
using namespace std;

int main() 
{
 int A, B, C, D, E, F, G, H, I;
 cin>>A>>B;
 cin>>C>>D>>E;
 cin>>F>>G>>H>>I;
 cout<<A<<" "<<B<<" "<<C<<" "<<D<<" "<<E<<" "<<F<<" "<<G<<" "<<H<<" "<<I;
  return 0;
}
.................................
#include <bits/stdc++.h>
using namespace std;

int main() 
{
 string A,B,C,D,E,F,G,H,I;
 cin>>A>>B;
 cin>>C>>D>>E;
 cin>>F>>G>>H>>I;
 cout<<A<<" "<<B<<" "<<C<<" "<<D<<" "<<E<<" "<<F<<" "<<G<<" "<<H<<" "<<I;
  return 0;
}
................................
#include <bits/stdc++.h>
using namespace std;

int main() 
{
 int a = 5;
 cout<<a<<endl;
 		
 string b = "abcde";		
 cout<<b<<endl;
		
 int array[] = {1,2,3}; 
 cout<<array[0]<<" "<<array[1]<<" "<<array[2];	
}
.....................................
#include <bits/stdc++.h>
using namespace std;

int main() 
{
 int N;
 string S;
 cin>>N;
 cin>>S;
 cout<<N<<" "<<S;
  return 0;
}
......................................

#include <bits/stdc++.h>
using namespace std;

int main() 
{
    int A, B, C, D, E;
    cin>> A;
     cin>>B;
    cin>> C;
    cin>> D;
    cin>> E;
    cout<<A << endl;
    cout<< B << endl;
    cout<<C << endl;
    cout<< D << endl;
    cout<< E <<endl;
}
..................................
using namespace std;

int main() 
{
 int t;
 //accept the count of test cases given in the the 1st line
 cin>>t;
 //Run a loop to accept 't' inputs
while(t--)
 { 
   int N;
 //accept an integer N in each test case
   cin>>N;
 //output the number mirror for each test case
   cout<<N<<endl;
  }  
 return 0;
}
.....................................
int main() 
{
 int t;
 //accept the count of test cases given in the the 1st line
 cin>>t;
 //Run a loop to accept 't' inputs
while(t--)
 { 
   int A,B,C,D,E;
 //accept 2 integers on the 1st line of each test case
   cin>>A>>B;
 //accept 3 integers on the 1st line of each test case
   cin>>C>>D>>E;
 //output the 5 integers on a single line for each test case 
   cout<<A<<" "<<B<<" "<<C<<" "<<D<<" "<<E<<endl;
  }
 return 0;
}
......................................
#include <bits/stdc++.h>
using namespace std;

int main() 
{
 int t;
 //accept the count of test cases given in the the 1st line
 cin>>t;
 //Run a loop to accept 't' inputs
while(t--)
 { 
   int A,B;
   string S;
 //accept 2 integers on the 1st line of each test case
   cin>>A>>B;
 //accept 1 string on the 2nd line of each test case
  cin>>S;
 //output the 5 integers on a single line for each test case 
   cout<<A<<" "<<B<<" "<<S<<endl;
  }
 return 0;
}
..........................................
#include <bits/stdc++.h>
using namespace std;

int main() 
{
 int t;

 cin>>t;

while(t--)
 { 
   int N;
   cin>>N;
   cout<<N+1<<endl;
  }
 return 0;
}
........................................
using namespace std;

int main() 
{
 int t;
 //accept the count of test cases given in the the 1st line
 cin>>t;
 //Run a loop to accept 't' inputs
while(t--)
 { 
   int N;
 //accept 1 integer on the 1st line of each test case
   cin>>N;
 //output the negative integer - i.e. (-N)
   cout<<-N<<endl;
  }
 return 0;
}
..........................
using namespace std;

int main() 
{
 int t;
 //accept the count of test cases given in the the 1st line
 cin>>t;
 //Run a loop to accept 't' inputs
while(t--)
 { 
   string S,X;
   cin>>S;
 //Store the value of string S concatenated with itself to variable X
   X=S+S;
   cout<<X<<endl;
  }
 return 0;
}
.......................................
#include <bits/stdc++.h>
using namespace std;

int main() 
{
 int t;
 cin>>t;
while(t--)
{
   int N;
   cin>>N;
   cout<<2*N<<endl;
  }
 return 0;
}
.................................
#include <bits/stdc++.h>
using namespace std;

int main() 
{
 int t;
 cin>>t;
 while(t--)
  { 
   int X,Y;
   cin>>X;
   //Create a new variable Y for total distance. 5 days a week - Chef will travel 2*X distance each day - i.e. return trip walk to office
    Y = 5 * 2 * X;  
   //Output distance traveled for each test case
    cout<<Y<<endl;
   }
 return 0;
}
.....................................
#include <bits/stdc++.h>
using namespace std;

int main() 
{
 int t;
 cin>>t;
 while(t--)
  { 
   int X,Y,prize_top10,prize_11to100;
   cin>>X>>Y;
   prize_top10 = 10*X;           
   prize_11to100 = 90*Y;        
   cout<<prize_top10+prize_11to100<<endl; 
   }
 return 0;
}
.................................

